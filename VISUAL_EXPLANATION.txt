# Crash Recovery Bug - Visual Explanation

## How the Bug Manifests

```
┌─────────────────────────────────────────────────────────────┐
│                    NORMAL OPERATION                          │
└─────────────────────────────────────────────────────────────┘

Index File (keys.index):          Data File (keys.dat):
┌──────────────┐                  ┌──────────────────┐
│ 0            │ ───────────────> │ (empty)          │ pos 0
├──────────────┤                  ├──────────────────┤
│ 100          │ ───────────────> │ "key1"           │ pos 0-99
├──────────────┤                  ├──────────────────┤
│ 200          │ ───────────────> │ "key2"           │ pos 100-199
├──────────────┤                  ├──────────────────┤
│ 300          │ ───────────────> │ "key3"           │ pos 200-299
└──────────────┘                  └──────────────────┘
                                   File size: 300 bytes ✓


┌─────────────────────────────────────────────────────────────┐
│                    CRASH OCCURS!                             │
│  (During write of key4, system crashes/killed)              │
└─────────────────────────────────────────────────────────────┘

What was supposed to happen:
1. Write "key4" data to position 300-399 ✗ (incomplete)
2. Update index to show position 400    ✓ (completed in memory)
3. Sync data file to disk              ✗ (never happened)
4. Sync index file to disk             ✓ (happened before crash)


┌─────────────────────────────────────────────────────────────┐
│                AFTER CRASH - CORRUPTION                      │
└─────────────────────────────────────────────────────────────┘

Index File (keys.index):          Data File (keys.dat):
┌──────────────┐                  ┌──────────────────┐
│ 0            │ ───────────────> │ (empty)          │
├──────────────┤                  ├──────────────────┤
│ 100          │ ───────────────> │ "key1"           │
├──────────────┤                  ├──────────────────┤
│ 200          │ ───────────────> │ "key2"           │
├──────────────┤                  ├──────────────────┤
│ 300          │ ───────────────> │ "key3"           │
├──────────────┤                  ├──────────────────┤
│ 400          │ ───────────> ☠️  │ (END OF FILE)    │
└──────────────┘                  └──────────────────┘
  ↑ Points beyond file!            File size: 300 bytes
  This causes the crash            BUT index says 400!


┌─────────────────────────────────────────────────────────────┐
│              THE CRASH - Original Code                       │
└─────────────────────────────────────────────────────────────┘

Function: readDataAtIndexPos()
├─ Read entry at position 300 from index
├─ dataPos = 300
├─ nextDataPos = 400 (from next index entry)
├─ dataFileLength = 300 (actual file size)
│
└─> if nextDataPos (400) > dataFileLength (300) {
        return error ❌
        ↓
        panicOnError() crashes entire service 💥
    }


┌─────────────────────────────────────────────────────────────┐
│              THE FIX - New Code                              │
└─────────────────────────────────────────────────────────────┘

Function: readDataAtIndexPos()
├─ Read entry at position 300 from index
├─ dataPos = 300
├─ nextDataPos = 400 (from next index entry)
├─ dataFileLength = 300 (actual file size)
│
└─> if nextDataPos (400) > dataFileLength (300) {
        log warning: "Corrupt index detected" ⚠️
        return nil, deleted=true, nil ✓
        ↓
        Caller treats as deleted entry
        ↓
        Database continues operating 🎉
    }


┌─────────────────────────────────────────────────────────────┐
│                   BEHAVIOR COMPARISON                        │
└─────────────────────────────────────────────────────────────┘

                Original Code              Fixed Code
┌─────────────────────────────────────────────────────────────┐
│ Corruption    │ Crashes entire        │ Logs warning       │
│ Detected      │ service 💥            │ ⚠️                 │
├─────────────────────────────────────────────────────────────┤
│ Corrupted     │ Inaccessible          │ Treated as         │
│ Entry         │ (service down)        │ deleted            │
├─────────────────────────────────────────────────────────────┤
│ Other         │ Inaccessible          │ Fully accessible   │
│ Entries       │ (service down)        │ ✓                  │
├─────────────────────────────────────────────────────────────┤
│ New Writes    │ Impossible            │ Continue working   │
│               │ (service down)        │ ✓                  │
├─────────────────────────────────────────────────────────────┤
│ Recovery      │ Manual intervention   │ Automatic,         │
│               │ required              │ immediate          │
└─────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│                 REAL WORLD EXAMPLE                           │
│              (Your Actual Error)                             │
└─────────────────────────────────────────────────────────────┘

Index says: nextDataPos = 388
Actual file size: 381 bytes
Difference: 7 bytes (likely partial write)

The index was updated to 388, expecting data up to position 388,
but the data file only has 381 bytes. The missing 7 bytes were
never written due to the crash.

With the fix:
- Entry at position 381-388 is skipped (treated as deleted)
- All entries before position 381 remain accessible
- New writes continue after position 381
- Service stays online ✓
